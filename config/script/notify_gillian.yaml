#####################################################################
#                      NOTIFY
#####################################################################
notify_gillian:
  alias: Notify Gillian
  sequence:
    - service: notify.mobile_app_iphone_13_pro_de_gillian
      data:
        title: '{{ title }}'
        message: '{{ message }}'
        data:
          push:
            sound:
              name: >
                {% if (sound is defined) and (sound != '') %}                  
                  {{ sound }}                  
                {% else %}
                  "default"
                {% endif %}
              critical: >
                {% if (critical is defined) and (critical != '') %}
                  {{ critical }}
                {% else %}
                  0
                {% endif %}
            volume: 5.0
  mode: single
